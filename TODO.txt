- Get the data only one time per day, then only read the local data: add last update date to the csv name
- Add a force get optional parameter if need to get the data from the web more than once a day
- Merge all the different format processes into one
- Have options to crawl the csv files with filters
- Add round number for all the games if available
